import { MainContainer } from './Main.styles';
import { PaginationComponent } from '../Pagination/Pagination';

export const Main = () => {
  return (
    <MainContainer>
      <PaginationComponent />
    </MainContainer>
  );
};
